---
title: Workshop Papers
---
<div class="content">
  <h3>Conference Papers</h3>
  <table>
    <tr>
      <th>Title</th>
      <th>Authors</th>
      <th>Workshop</th>
    </tr>

    <% data.workshop.workshop.each do |p| %>
      <tr>
        <% if p.link == "" %>
          <td> <%= p.title %> </td>
        <% else %>
          <td> <a href="<%= p.link %>"> <%= p.title %> </a></td>
        <% end %>
        <td> <%= p.authors %> </td>
        <td> <%= p.where %> </td>
      </tr>
    <% end %>
  </table>
</div>
